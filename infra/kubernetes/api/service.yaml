apiVersion: v1
kind: Service

metadata:
  name: matriculas-api
  namespace: matriculas

spec:
  selector:
    app: matriculas-api

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP